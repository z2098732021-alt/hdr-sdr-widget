{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "diag",
  "description": "诊断窗口。只需读应用版本号 —— 采集统计 / 渲染端状态都走应用自有命令，不受 ACL 管辖；帧预览走 glass 自定义协议 fetch，也不经 ACL。",
  "windows": ["diag"],
  "permissions": ["core:app:allow-version"]
}
